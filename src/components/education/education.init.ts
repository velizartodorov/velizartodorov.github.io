import { Period } from '../common/period';

export interface IEducation {
    occupation: string;
    institution: string;
    place: string;
    icon: string;
    period: Period;
    body: string;
}
